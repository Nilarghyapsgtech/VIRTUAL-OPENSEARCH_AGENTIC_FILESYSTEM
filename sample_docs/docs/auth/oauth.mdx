# OAuth

OAuth is used for delegated authorization.

## Configure an OAuth application

1. Create an application in the identity provider.
2. Configure the OAuth redirect URI.
3. Store the client ID and client secret securely.

## Token exchange

The API exchanges an authorization code for an access token.
OAuth scopes are validated before token exchange.
