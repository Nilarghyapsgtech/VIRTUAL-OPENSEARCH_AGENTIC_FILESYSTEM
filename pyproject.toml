[project]
name = "opensearchfs-fastapi"
version = "0.1.0"
description = "Agent-facing virtual filesystem over OpenSearch implemented with FastAPI."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
